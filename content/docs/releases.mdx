---
title: Releases
description: What changed, and what you have to change with it.
---

`+` is new, `-` is gone, `!` is something you have to know about before you upgrade.

## 4.0.2

```diff
+ Folding a log is about a fifth faster, and every read folds one
+ A Future holds less than half the memory it used to
+ An Observer sends a value about three times faster

- A transfer name used by two senders, where a record written before 4.0.1 could settle the wrong hold
- Session:Apply answering Refused for an op that only holds once a parked transaction goes through

! An Observer sends to its listeners in the order they subscribed
```

### Upgrading

Drop it in. A delivery now says who sent it from the delivery itself rather than from its name, so a
record written by an older build is read the same way as a new one.

`Session:Apply` answers `Busy` or `Unresolved` in one more place. An op that the reducer turns away
now, but would take once a parked transaction goes through, answers
[`Unresolved`](/docs/concepts/reasons) instead of [`Refused`](/docs/concepts/reasons). `Store:Edit`
already answered that way, so the two agree now. Ask again once the transaction settles rather than
telling the player no.

## 4.0.1

```diff
- Two senders using one transfer name, where one of the amounts was destroyed and both calls said true
- Recovery settling a hold against a delivery that came from a different sender
- Reset dropping units set aside for another key, so a grant could leave them on both
- A commit refused by a newer write staying in the log and stopping that key compacting for good

! A transfer name now means the sender as well as the amount and the receiver
! Reset answers Busy while the key holds units set aside for another key
! A compaction holds back a reserved op this build does not handle, the same as it does for yours
```

### Upgrading

A transfer name belongs to one sender now. Two senders were never meant to share one, and doing it
used to destroy an amount without saying so. It answers `Spent` instead.

Handle `Busy` from `Reset` if you call it. A key holding units for another key refuses the reset the
same way a key with a transaction parked on it already did. Hand the units over or release them
first.

## 4.0.0

```diff
+ Ledger.UseClock takes a second argument, the wait Ledger polls on

! The files inside the module were all moved and renamed
```

Complete rewrite, cleaner code, same api, less lines

## 3.4.0

```diff
+ Store:Reserve, Confirm and Release set units of a numeric field aside on one key
+ Store:Grant hands units set aside on one key over to another
+ Store:Bump and Total keep a running count that many servers can add to at once
+ Held is a new reason, for units that left the sender and are on their way back
+ Tx costs 8 requests for 2 legs and 12 for 4, it used to be 10 and 16

- During a deploy, an older build compacting a key and dropping a write the newer build had made
- Erase and Reset throwing away a transaction leg the transaction still counted as committed
- Any write to an erased key cancelling its tombstone, so money could be sent to it again
- Transaction markers piling up until cleanup stopped running altogether
- Markers for a transaction across two stores never being tidied by a server that builds only one
- Session:Apply accepting an op that only holds if two parked transactions both go the same way
- Sessions kept for the life of the server when a player left part way through loading

! Every applied name ages out after 30 days now, transaction ids included
! A transfer into an erased key answers Held, it used to answer Unresolved
! Erase and Reset answer Busy while a transaction is parked on the key
! An erased key keeps its tombstone for 8 days even if something writes to it
! A session that loaded before an erase stops writing and closes itself, and says so
! A Store:Edit your reducer refuses is no longer written to the log, so Inspect will not show it
! A compacted record carries the size of its snapshot, which older builds carry along untouched
! An idle session reads every 2 minutes rather than every 30 seconds
! A large profile compacts a quarter as often, and folds up to 512 ops instead of 128
! A build that cannot read a record refuses to write to it and answers Behind
```

The new methods are optional and nothing else depends on them. See
[Reservations and totals](/docs/guides/reservations) for what they are for and when to use one instead
of a transaction.

### Upgrading

Treat a `Transfer` that answers [`Held`](/docs/concepts/reasons) the way 3.3.0 told you to treat
`Unresolved` on that path. The money left the sender and comes back on its own. `Held` says only that,
where `Unresolved` also covers cases Ledger cannot answer at all.

Get a player off every server before erasing them. A tombstone now holds for its full 8 days. A write
no longer cancels it, and a session that loaded before the erase stops writing and closes itself.
Ledger warns in both cases. Nothing resurrects an erased key any more.

Derive transaction ids from the thing being settled. They age out after 30 days like every other
name, so reusing one a month later applies it again. 3.3.0 kept them for good. That is what made the
applied name set grow without bound on a key many transactions touch.

Records this build writes read fine on 3.3.0 and the other way round. A compacted record now carries
`Bytes`, the size of its snapshot. An ordinary write reads that rather than measuring the whole
snapshot to decide whether the log is worth folding in. A build that does not know the field carries
it along untouched, and one that does falls back to measuring when it is absent.

## 3.3.0

```diff
+ A name reused for different terms answers Spent on Tx and Transfer, it used to answer true
+ Store:Edit answers Full when a key carries more unsaved ops than it can hold
+ Session:Apply answers Unresolved or Busy when a parked transaction could change the answer
+ The mock stores a buffer, so a game that packs its own bytes can test against it
+ Ledger.HistoryEntry is exported, so a Store:History result can be typed
+ A mutating reducer throws on the Edit and Tx paths too, it used to only throw on Apply

- Reuse of a transfer or transaction name answering true and moving nothing
- A committed transaction applying a second time once its record aged out
- Transfer calling it Refused while the money sat in escrow waiting to come back
- Reset restoring a payload no migration ever ran on
- Keys of few but very large ops never compacting
- A refused transfer using up its name
- State that was frozen at the top level but writable underneath
- A buffer in Default being shared by every key that had not stored one yet
- Recovery gaps around parked transactions and cleared delivery evidence
- A build time warning about migrations that was wrong for every rename

! ClearDelivered keeps a delivery id for 30 days, it used to drop it after 8
! A transaction id is kept for good, transfer ids and Once names still age out at 30 days
! A transfer into a key erased inside the tomb window answers Unresolved, it used to answer Refused
! A Once name that is empty or not a string answers Invalid, it used to throw
! Store:RecoverTransfers answers false when it could not take every held transfer
! Deploy every server before you rely on the new answers
```

### Upgrading

Pass the same terms on a retry. A name that already ran now answers [`Spent`](/docs/concepts/reasons)
if you ask again for different keys or a different amount, however long ago the first one ran. The same
terms still answer `true` and still move nothing.

Handle the new reasons. `Session:Apply` can answer [`Unresolved`](/docs/concepts/reasons) or
[`Busy`](/docs/concepts/reasons) while a transaction is parked on the key, `Store:Edit` can answer
[`Full`](/docs/concepts/reasons), and a bad `Once` name answers [`Invalid`](/docs/concepts/reasons)
where it used to throw. Treat each the way you already treat that reason elsewhere.

Call `Store:RecoverTransfers` again while it answers [`Busy`](/docs/concepts/reasons). It handles 32
held transfers a call and used to answer `true` regardless.

Treat a `Transfer` that answers [`Unresolved`](/docs/concepts/reasons) as money in flight, not money
untouched. Sending into a key erased in the last 8 days used to answer `Refused`, which promises
nothing changed. The amount is set aside and comes back on its own, so don't refund the player
yourself and don't send it again under a new name.

Finish the deploy before you rely on the new answers. Records this build writes read fine on 3.2.0,
which never expires their applied names, so that set grows until every server is upgraded. Nothing is
lost and nothing applies twice.

Everything else here is a fix you do not have to act on.


## 3.2.0

```diff
+ Flush, Compact and Release name the reason they failed
+ Store:Resettle is part of the api, and handles its key like every other method

! Store:Resettle hands back a Future now, so it needs :Wait()
! Tx answers Invalid for a leg field it cannot store, it used to throw
```

### Upgrading

`Flush`, `Compact` and `Release` return a reason next to the boolean. The boolean means what it
always meant, so you can ignore the reason and change nothing. See
[Session](/docs/reference/session#flush).

`Resettle` needs a change if you were calling it. It was reachable before but never declared, and it
took a key string through without checking it. It is on `Store` now. It resolves the key the way
`Peek` and `Edit` do, so a bad one throws where you wrote it, and it answers
`Future<boolean, Reason?>`. Add `:Wait()`. `true` means nothing is left unfinished on the key. `Busy`
means a transaction leg is still waiting on a decision, so try again later. See
[Resettle](/docs/reference/store#resettle).

`Tx` used to throw when a leg's `Fields` held something a datastore can't take. It answers
[`Invalid`](/docs/concepts/reasons) now, the same as `Edit` already did for the same content. Leg
fields are usually calculated, so `Fields = { Amount = Price * Count }` can produce a NaN, and that
used to crash the caller. If you wrapped `Tx` in a `pcall` for this, read the reason instead. The
shape of a leg still throws, so a bad key, a duplicate key or a `Once` on a leg is unchanged.

## 3.1.2

Op ids got shorter, and there is a way to read the record on a key rather than the state it folds to.

```diff
+ Buffers can live in state and in ops
+ Store:Inspect, the record on a key rather than the state it folds to
+ Op ids named after the server and counted, half the length of the old ones
```

### Upgrading

A buffer in state is copied rather than shared, so one session writing into one can't reach another.
Freezing can't cover it though, since Luau has no way to freeze a buffer, so build a new one rather
than writing into the one your reducer was handed. See
[Writing a reducer](/docs/concepts/reducer#it-has-to-not-mutate).

Op ids changed shape and nothing has to be done about it. An id is opaque, compared only for equality,
so ids written by older builds keep working next to the new ones and there is no migration. If you
were reading op ids expecting a GUID, they now look like `3c404902bf6a4a4e:41`.

## 3.1.1

Erasing a key could destroy money that was still on its way to it, and a failed load had one
answer for every reason it might have failed.

```diff
+ OnLoadFailed, so a failed load can teleport or retry instead of always kicking
+ Erase leaves a tombstone, so money still on its way in goes back to whoever sent it
+ Making ledger smarter, being able to spot future failures quicker

- A transfer landing mid erase being destroyed with the sender already settled

! Erase leaves the key in place as a tombstone for 8 days, it used to delete it
! Sending to a key that was erased answers Refused rather than going through
```

### Upgrading

`Erase` is the one you can see from outside. It leaves a tombstone rather than deleting the key, so a
transfer already on its way in gets refused and refunded. Reads of an erased key are unchanged, they
still come back as a fresh profile, and any ordinary write clears the tombstone. If you have tooling
that checks the key is really gone, call `Erase` a second time after the 8 days and it removes it.
See [Erase](/docs/guides/recovery#erase).

`OnLoadFailed` is opt in and the default is the kick you already had, so nothing changes unless you
pass it. Pass it if you deploy while people are playing, since `Behind` wants a teleport and a rejoin
can't fix it. See [Ledger.New](/docs/reference/ledger#onloadfailed).

## 3.1.0

Every method that could fail now says why. Before this, a read that failed handed you back nothing
at all, which arrived in your variable as `nil` and looked exactly like a player with no data.

```diff
+ Every read says why it failed, the same way every write already did
+ Behind, a ninth reason, for a record a newer build wrote
+ Erase, RecoverTransfers and ClearDelivered say whether they ran

- Reads that failed by handing back nothing, with no way to ask what went wrong
- A datastore blip and a record this server must not touch looking the same
- A failed DidApply reading as never granted, so a support tool pays twice

! Reads answer (value?, Reason?), and nil always means the read failed
! Store:DidApply can be nil, so compare it against true rather than truthiness
! Reset on a record from a newer build says Behind, it used to say Refused
! Writes dropped by the shutdown say Closed, they used to say Unresolved
```

### Upgrading

Most code keeps working. A missing first return was already `nil`, so `if Ok then` and
`if State == nil then` behaved the same before and after. What changes is that you can now tell the
failures apart, and strict mode will point at the call sites worth revisiting.

The one that can cost you money is `DidApply`:

```luau
-- before, and now wrong: a failed read reads as never granted, so you grant again
if Store:DidApply(UserId, Name):Wait() then

-- now
if Store:DidApply(UserId, Name):Wait() == true then
```

And if you match on reasons to decide whether to retry, add `Behind` to the "don't bother" side. It
means this server is running an older build than the one that wrote the record, so retrying can't
help and writing a fresh profile over the top would destroy real data. See
[Reasons](/docs/concepts/reasons).

## 3.0.1

```diff
+ Edit and Reset prove their answer against a pending transaction instead of guessing
+ Markers on keys holding a delimiter byte reap like any other
+ OnceAt joins Id and Kind as reserved, overwritten with a warning
+ Warnings rewritten in plain speech, what happened and what to do about it

- A marker naming a key with \0 or \1 in it could never be collected by anyone
- A forged OnceAt could wipe the applied id set and pay a delivered transfer twice
- Edit answering true for a verdict a pending leg was about to overturn
- Reserve reporting money set aside without saying on what terms
- Loading and Leaving as two maps that could disagree about one player

! Edit and Reset answer Unresolved when a stuck leg could still flip the verdict
! OnceAt in Fields is overwritten now, CommitOp still honours an op's own stamp
! Mid deploy, a transaction retried across versions can answer Spent once, nothing applies
```
